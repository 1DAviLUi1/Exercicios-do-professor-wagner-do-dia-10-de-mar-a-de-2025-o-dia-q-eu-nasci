#include <stdio.h>
int fatorial (int n){
    if(n == 0){
        return 1;
    }
    return n * fatorial(n - 1);
}
int main()
{
    int fat, n;
    printf(" DIGITE UM NUMERO: ");
    scanf(" %d", &n);
    
    fat = fatorial(n);
    
    printf("\n fatorial = %d", fat);

    return 0;
}
