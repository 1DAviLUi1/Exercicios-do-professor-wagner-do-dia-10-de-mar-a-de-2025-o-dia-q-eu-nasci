#include <stdio.h>
int main(){
    int loop;
    int scrlk;
    loop = 0;
    while(loop <30){
        loop++;
        if(loop %3==0){
            printf(":L\n");
       }
       else{
        printf(" %d\n", loop);
       }
  }
}
