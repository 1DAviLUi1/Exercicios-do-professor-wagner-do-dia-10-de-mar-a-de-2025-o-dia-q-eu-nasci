// Online C compiler to run C program online
#include <stdio.h>

int main() {
    float altura;
    int peso;
    float resultado;
    
    printf("WARNING WARNING WARNING!!!!!!!!!\n Lembrese de colocar um sua altura em metros (usando ponto e nao virgula)\n");
    while(1){
    printf("Digite seu peso:");
    scanf(" %d", &peso);
    printf("Digite altura:");
    scanf(" %f", &altura);
    resultado = peso / (altura * altura);
    if (altura >8){
        printf("O.o\n");
        printf("tu nao tem essa altura nem FODENDO meu mano, ou tu colocou errado ou voce nao existe, larga a mao de ser um burro de merda e faz direito essa porra\n");
        continue;
    }
    else{
        printf(" seu imc é:%f", resultado);
    }
    }
}
